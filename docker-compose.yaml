version: '3.8'

services:
  nuxt-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9002:9002" # Map port 9002 of the container to port 9002 on the host
    networks:
      - web
    # environment:
    #   PORT: 9002
    volumes:
      - .:/app # Optional: Mount local directory for live updates (not recommended for production)
      - /app/node_modules # Prevent overwriting node_modules by the volume
  nginx:
    image: nginx
    container_name: nginx-container
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8080:80" # Map container's port 80 to host's port 8080
    networks:
      - web

networks:
  web:
    driver: bridge